import React from 'react';
import {Card, CardTitle} from 'material-ui/Card';


const HomePage = () => (
    <Card className="container">
        <CardTitle title="PlayQ Task" subtitle="Image Uploader, signup to use service ."/>
    </Card>
);

export default HomePage;
